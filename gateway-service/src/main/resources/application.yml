server:
  port: 9000
spring:
  application:
    name: gateway-service
  cloud:
    gateway:
      routes:


eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:8888/eureka/
  instance:
    prefer-ip-address: true
