apiVersion: v1
kind: Service
metadata:
  name: teacher-client
  namespace: jwts
spec:
  selector:
    app: teacher-client
  ports:
    - name: http
      port: 9012
      targetPort: 9012
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: teacher-client
  namespace: jwts
spec:
  replicas: 1
  selector:
    matchLabels:
      app: teacher-client
  template:
    metadata:
      labels:
        app: teacher-client
    spec:
      containers:
        - name: teacher-client
          image: jwts/teacher-client:v1.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 9012
          env:
            - name: JAVA_OPTS
              value: "-Xms128m -Xmx384m"
          resources:
            requests:
              cpu: 100m
              memory: 512Mi
            limits:
              cpu: "2"
              memory: 1Gi
          readinessProbe:
            tcpSocket:
              port: 9012
            initialDelaySeconds: 120
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: 9012
            initialDelaySeconds: 180
            periodSeconds: 20
