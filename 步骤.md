需要。containerd 镜像存储在每个节点本地，Master 构建出的镜像不会自动同步到两个 Worker。

不需要在 Worker 上重新复制 JAR、Dockerfile并重新构建。推荐在 Master 导出镜像，然后把镜像文件传到两个 Worker 并导入。

下面假设：

```text
Master：192.168.1.100
Worker1：192.168.1.101
Worker2：192.168.1.102
用户名：ubuntu
项目目录：~/jwts/jar
```

请替换为你的实际 IP 和用户名。

**一、在 Master 导出镜像**

进入目录：

```bash
cd ~/jwts/jar
```

确保脚本有执行权限：

```bash
chmod +x export-images.sh import-images.sh
```

导出 8 个镜像：

```bash
sudo ./export-images.sh
```

默认会生成 `jwts-images` 目录：

```bash
ls -lh jwts-images/
```

应该看到：

```text
course-client-v1.0.tar
course-service-v1.0.tar
eureka-service-v1.0.tar
gateway-service-v1.0.tar
student-client-v1.0.tar
student-service-v1.0.tar
teacher-client-v1.0.tar
teacher-service-v1.0.tar
```

如果脚本提示找不到镜像，检查：

```bash
sudo nerdctl -n k8s.io images | grep jwts
```

**二、从 Master 复制到 Worker**

先在两个 Worker 创建目录：

```bash
ssh ubuntu@192.168.1.101 "mkdir -p ~/jwts"
ssh ubuntu@192.168.1.102 "mkdir -p ~/jwts"
```

复制镜像和导入脚本到 Worker1：

```bash
scp -r jwts-images import-images.sh ubuntu@192.168.1.101:~/jwts/
```

复制到 Worker2：

```bash
scp -r jwts-images import-images.sh ubuntu@192.168.1.102:~/jwts/
```

如果 SSH 用户是 `root`：

```bash
scp -r jwts-images import-images.sh root@192.168.1.101:/root/jwts/
scp -r jwts-images import-images.sh root@192.168.1.102:/root/jwts/
```

**三、在 Worker1 导入镜像**

登录 Worker1：

```bash
ssh ubuntu@192.168.1.101
cd ~/jwts
```

修复换行符并添加权限：

```bash
sed -i 's/\r$//' import-images.sh
chmod +x import-images.sh
```

导入镜像：

```bash
sudo ./import-images.sh
```

检查：

```bash
sudo nerdctl -n k8s.io images | grep jwts
```

应显示 8 个 `jwts/...:v1.0` 镜像。

退出 Worker1：

```bash
exit
```

**四、在 Worker2 导入镜像**

```bash
ssh ubuntu@192.168.1.102
cd ~/jwts

sed -i 's/\r$//' import-images.sh
chmod +x import-images.sh
sudo ./import-images.sh

sudo nerdctl -n k8s.io images | grep jwts
exit
```

也可以使用 Kubernetes 容器运行时工具检查：

```bash
sudo crictl images | grep jwts
```

两个 Worker 都必须显示全部 8 个镜像。

**五、检查 Kubernetes 节点**

回到 Master：

```bash
kubectl get nodes -o wide
```

确保三个节点状态均为：

```text
Ready
```

检查节点是否有污点：

```bash
kubectl describe nodes | grep -E 'Name:|Taints:'
```

通常 Master 有 `NoSchedule` 污点，所以 Pod 主要运行在两个 Worker 上。这也是两个 Worker 都必须导入镜像的原因。

**六、确认数据库已经准备好**

检查 MySQL：

```bash
kubectl get pods,svc -n default | grep mysql
```

检查数据库表：

```bash
MYSQL_POD=$(kubectl get pods -n default -o name | grep mysql | head -n 1)

kubectl exec -it -n default "$MYSQL_POD" -- \
  mysql -uroot -proot -e "USE jwts_db; SHOW TABLES;"
```

应该显示：

```text
t_course
t_enrollment
t_student
t_teacher
```

如果数据库尚未导入：

```bash
kubectl exec -i -n default "$MYSQL_POD" -- \
  mysql -uroot -proot --default-character-set=utf8mb4 < ~/jwts_db.sql
```

**七、检查部署 YAML**

进入 Master 上的项目目录：

```bash
cd ~/jwts/jar
```

检查文件：

```bash
ls k8s
```

执行客户端语法检查：

```bash
kubectl apply --dry-run=client -f k8s/
```

如果没有报错，再开始部署。

**八、创建命名空间并部署 Eureka**

```bash
kubectl apply -f k8s/namespace.yaml
kubectl apply -f k8s/eureka-service.yaml
```

等待 Eureka 启动：

```bash
kubectl rollout status deployment/eureka-service -n jwts --timeout=180s
kubectl get pods -n jwts -o wide
```

检查日志：

```bash
kubectl logs -n jwts deployment/eureka-service --tail=100
```

从浏览器访问：

```text
http://任意节点IP:30001
```

例如：

```text
http://192.168.1.101:30001
```

**九、部署三个业务 Service**

```bash
kubectl apply -f k8s/student-service.yaml
kubectl apply -f k8s/teacher-service.yaml
kubectl apply -f k8s/course-service.yaml
```

等待启动：

```bash
kubectl rollout status deployment/student-service -n jwts --timeout=180s
kubectl rollout status deployment/teacher-service -n jwts --timeout=180s
kubectl rollout status deployment/course-service -n jwts --timeout=180s
```

检查状态：

```bash
kubectl get pods,svc -n jwts -o wide
```

如果某个服务启动失败：

```bash
kubectl logs -n jwts deployment/student-service --tail=200
kubectl logs -n jwts deployment/teacher-service --tail=200
kubectl logs -n jwts deployment/course-service --tail=200
```

**十、部署三个 Client 服务**

```bash
kubectl apply -f k8s/student-client.yaml
kubectl apply -f k8s/teacher-client.yaml
kubectl apply -f k8s/course-client.yaml
```

等待启动：

```bash
kubectl rollout status deployment/student-client -n jwts --timeout=180s
kubectl rollout status deployment/teacher-client -n jwts --timeout=180s
kubectl rollout status deployment/course-client -n jwts --timeout=180s
```

**十一、最后部署 Gateway**

```bash
kubectl apply -f k8s/gateway-service.yaml
kubectl rollout status deployment/gateway-service -n jwts --timeout=180s
```

查看全部资源：

```bash
kubectl get pods,svc,deployments -n jwts -o wide
```

Eureka 页面中应看到其他服务注册成功。

访问网关：

```text
http://任意节点IP:30000
```

测试接口：

```bash
curl http://192.168.1.101:30000/api/students
curl http://192.168.1.101:30000/api/teachers
curl http://192.168.1.101:30000/api/courses
```

具体路径需要以项目 Controller 中定义的接口为准。

**十二、常见故障排查**

出现 `ImagePullBackOff`：

```bash
kubectl describe pod -n jwts POD名称
sudo nerdctl -n k8s.io images | grep jwts
```

通常表示 Pod 所在 Worker 没有对应镜像。查看 Pod 所在节点：

```bash
kubectl get pods -n jwts -o wide
```

出现 `CrashLoopBackOff`：

```bash
kubectl logs -n jwts POD名称 --previous
kubectl describe pod -n jwts POD名称
```

检查最近事件：

```bash
kubectl get events -n jwts --sort-by=.metadata.creationTimestamp
```

重新部署更新后的镜像时，需要在每个 Worker 重新导入，然后重启 Deployment：

```bash
kubectl rollout restart deployment -n jwts
kubectl get pods -n jwts -w
```

由于 YAML 使用了 `imagePullPolicy: IfNotPresent`，每个可能调度 Pod 的节点都必须提前存在对应的 `jwts/...:v1.0` 镜像。